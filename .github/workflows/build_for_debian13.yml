name: build_for_debian13

on:
    workflow_dispatch: 
    
jobs:
  build:

    runs-on: ubuntu-latest

    

    steps:
     - name: create folder
       run: |
        mkdir dist
     
     
     - name: build with docker 
       run: | 
          docker run \
            -u root:root \
            -e BRANCH_BUILD=main\
            -v $(pwd)/dist/:/home/builduser/dist \
            sgngodin/buildtestbraillerapdebian13:latest

     - name: Upload artifact
       uses: actions/upload-artifact@v4.3.3
       with: 
              name: testbraillerap-debian13
              path: ./dist/testbraillerap-debian13-*